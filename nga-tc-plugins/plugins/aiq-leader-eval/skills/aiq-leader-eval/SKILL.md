---
name: aiq-leader-eval
description: AI-Q 리더 평가 가이드 HTML을 부서별로 생성하는 스킬. NGA 자가진단 설문 응답(CSV/TSV/Excel)을 입력받아 코어비즈에서 사용한 v6 양식(4축 × 15 sub-item, 26명 신호 매트릭스, KPI 대시보드, 멤버별 점수 입력 + 엑셀 내보내기)과 동일한 단일 HTML 파일을 출력한다. '리더 평가 가이드 만들어줘', 'AI-Q 평가 HTML', '부서 AIQ 평가지', '코어비즈 양식으로 다른 부서', '자가진단 결과 평가지', '리더용 평가 시트' 등의 표현이 나오면 반드시 이 스킬을 사용한다. 부서명과 설문 파일만 받으면 자동으로 신호 분류·집계·HTML 생성을 끝낸다.
---

# AI-Q 리더 평가 가이드 생성 스킬

NGA AX 프로그램에서 사용하는 표준 평가지 v6를 다른 부서에 동일 양식으로 재현하는 스킬. 잭(NGA)이 코어비즈 26명 대상으로 설계·검증한 구조를 그대로 유지한다.

## 입력

1. **설문 응답 파일** — `/mnt/user-data/uploads/`에 있는 CSV / TSV / Google Form 익스포트
2. **부서명** — 예: "코어비즈", "비전비즈", "소호사업부", "마케팅"

설문 컬럼이 코어비즈 양식과 동일하지 않을 수 있다. 컬럼 매핑은 `references/column_mapping.md`에 정의된 키워드 기반으로 자동 매칭한다.

## 출력

`/mnt/user-data/outputs/{부서명}_AIQ_리더평가가이드.html` — 단일 HTML 파일 (Chart.js·SheetJS CDN 사용)

## 표준 워크플로우

작업 순서는 반드시 다음을 따른다.

### Step 1. 입력 파악

설문 파일 경로와 부서명을 확인한다. 부서명이 명시되지 않으면 파일명에서 추론하거나 사용자에게 묻는다. CSV/TSV는 `pandas`로 읽고, Excel은 `openpyxl`로 읽는다. 첫 행이 헤더가 아닐 가능성에 주의 — Google Form 익스포트는 첫 행이 질문 본문이다.

### Step 2. 컬럼 매핑

`references/column_mapping.md`의 매핑 규칙을 읽어 응답 컬럼을 표준 키(q1.3, q2.1, q2.2 … q8.7)로 변환한다. 키워드 기반 부분 일치로 매핑하며, 매칭 실패한 컬럼은 사용자에게 확인 요청 후 진행한다.

### Step 3. 신호 분류

각 응답자에 대해 6가지 신호(01툴 / 01빈도 / 02사례 / 02검토 / 03임팩트 / 04공유)를 자동 분류한다. 분류 규칙은 `references/signal_rules.md`에 정의되어 있다. 잭이 코어비즈 26명에게 매긴 실제 패턴에서 역추출한 규칙이므로 그대로 적용해야 일관성이 유지된다.

### Step 4. 집계 데이터 생성

대시보드용 6가지 집계 — `tool_usage`, `freq_dist`, `share_dist`, `time_saved_dist`, `auto_count_dist`, `difficulty_top` — 와 KPI 5종 — `respondents`, `regular_users`, `automation_owners`, `non_sharers`, `time_savers` — 를 계산한다.

### Step 5. HTML 생성

`scripts/build_aiq_html.py`를 호출한다. 스크립트는 매핑된 데이터·신호·집계를 받아 잭의 v6 템플릿(헤더 → 액션바 → 가이드 → 대시보드 → 요약 테이블 → 멤버 탭 → 멤버 상세 × N → JS)을 그대로 재현한다.

```bash
python3 scripts/build_aiq_html.py \
  --input /mnt/user-data/uploads/{설문파일} \
  --department "{부서명}" \
  --output /mnt/user-data/outputs/{부서명}_AIQ_리더평가가이드.html
```

### Step 6. 결과 제시

`present_files`로 출력 파일을 공유한다. 매핑 실패·신호 분류 경계 케이스가 있었다면 1~2줄로만 짚어준다(긴 보고는 불필요).

## 4축 구조 (변경 금지)

코어비즈에서 확정된 구조. 부서별로 가중치·sub-item을 변경하지 않는다.

| 축 | 가중치 | Sub-item | 근거 질문 |
|---|---|---|---|
| 01 AI 툴 활용 | 25% | s11 비중 / s12 도구 다양성 / s13 정착 깊이 / s14 신규 시도 | q1.3 / q2.1 / q2.2 / q2.3 |
| 02 결과물 완성도 | 30% | s21 사례 구체성 / s22 검증 흐름 / s23 오류 수정 / s24 체크리스트 | q3.1~q3.6 / q4.1 / q4.2 / q4.3 |
| 03 속도·임팩트 | 30% | s31 자동화 수 / s32 절약 시간 / s33 새 시도 / s34 AI 없으면 못 한 | q5.1 / q5.2 / q5.3 / q5.4 |
| 04 지식 공유 | 15% | s41 채널 다양성 / s42 동료 변화 / s43 자료 흔적 | q6.1 / q6.2 / q6.3 |

총점 = (a1·25% + a2·30% + a3·30% + a4·15%) × 20, 등급 S(85+) / A(70~85) / B(55~70) / C(<55)

## 신호 색상 코드

- 🟢 sig-green: 정착·체화 — 구체 사례·다양성·일상 흐름
- 🟡 sig-yellow: 진행 — 신호 있으나 한정적
- 🔴 sig-red: 시작·간헐 — 사례·정착 부족

## 참고 파일

- `scripts/build_aiq_html.py` — 입력 파일을 받아 HTML을 생성하는 메인 스크립트. 컬럼 매핑·신호 분류·집계·HTML 빌딩을 모두 포함한다.
- `references/column_mapping.md` — 설문 컬럼명 → 표준 키 매핑 규칙
- `references/signal_rules.md` — 6가지 신호의 분류 로직 (코어비즈 26명에서 역추출)
- `references/axis_schema.md` — 4축 sub-item 정의와 evidence question 매핑

## 자주 빠지는 함정

- **컬럼 누락**: 부서별로 일부 질문이 빠질 수 있다. q4.3(체크리스트), q5.5(산출물 링크), q6.2(동료 변화), q6.3(자료 링크)는 응답률이 낮은 경향. 빈 응답은 "응답 없음"으로 표시하고 점수 영향은 sub-item 단위에서 결정되도록 둔다.
- **이름 중복**: 같은 부서 내 동명이인은 거의 없지만, 이메일이 식별자로 더 안전하다. 정렬은 응답 순서 또는 직책(팀장→파트리더→매니저→MD→AMD→사원) 순.
- **단일 응답자 부서**: 1~2명 부서는 대시보드 차트가 의미 없다. 그래도 동일 양식을 유지하되, KPI 카드만 표시하고 차트는 "응답자 수 부족" 메시지를 띄운다.
- **신호 보조 정보**: 멤버 상세에서 각 축마다 보조 시그널(예: "🟡 복수 — 2종 · 보조 신호 🟢 체화")이 같이 나온다. 이건 메인 신호 옆에 보조 축 정보를 한 번 더 보여주는 거다. 빌더 스크립트가 자동 생성한다.
